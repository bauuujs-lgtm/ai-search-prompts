# GitHub Pages 上线步骤

你已经登录 GitHub 的情况下，按这个步骤做。

## 1. 创建仓库

1. 打开 GitHub 首页。
2. 右上角点 `+`。
3. 点 `New repository`。
4. Repository name 填：

```text
ai-search-prompts
```

5. 选择 `Public`。
6. 不要勾选 `Add a README file`。
7. 点 `Create repository`。

## 2. 上传网站文件

进入新仓库后：

1. 点 `uploading an existing file`。
2. 打开本地文件夹：

```text
E:\zdhjq\ai-search-prompts
```

3. 把里面这些文件和文件夹全部拖进去：

```text
about.html
data
index.html
README.md
robots.txt
script.js
sitemap.xml
styles.css
```

4. 页面底部 Commit message 写：

```text
Initial AI search prompts site
```

5. 点 `Commit changes`。

## 3. 开启 GitHub Pages

1. 进入仓库的 `Settings`。
2. 左侧点 `Pages`。
3. Source 选择 `Deploy from a branch`。
4. Branch 选择 `main`。
5. Folder 选择 `/ (root)`。
6. 点 `Save`。

等 1-3 分钟，页面会出现网址，通常是：

```text
https://你的用户名.github.io/ai-search-prompts/
```

## 4. 上线后要改的地方

拿到网址后，把网址发给我。我会帮你把这些文件里的 `https://example.com/` 改成真实地址：

- `index.html`
- `robots.txt`
- `sitemap.xml`

## 5. 后续收录

上线后可以继续做：

- 提交百度搜索资源平台。
- 提交 Bing Webmaster。
- 提交 Google Search Console。
- 每天新增 5-10 条 AI 搜索指令。

